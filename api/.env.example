# NestJS — Variables de entorno
# Copia este archivo como .env y completa los valores

# Puerto del servidor
PORT=3000

# CORS — origins permitidos (separados por coma)
CORS_ORIGIN=http://localhost:4200

# Firebase Admin SDK
# Descargar desde: Firebase Console → Configuración del proyecto → Cuentas de servicio → Generar nueva clave privada
FIREBASE_PROJECT_ID=appnotesbg-app
FIREBASE_CLIENT_EMAIL=           # Del service account JSON
FIREBASE_PRIVATE_KEY=            # Del service account JSON (incluir \n literales)

# Algolia (Admin API Key — NUNCA exponer en frontend)
ALGOLIA_APP_ID=
ALGOLIA_ADMIN_API_KEY=
ALGOLIA_INDEX_NAME=notes_dev

# Google Gemini API (post-MVP)
GEMINI_API_KEY=
