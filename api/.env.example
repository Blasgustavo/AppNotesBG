# NestJS — Variables de entorno
# Copia este archivo como .env y completa los valores
# ⚠️  NUNCA subir .env a Git — está en .gitignore
# ⚠️  NUNCA usar service-account.json en disco — usar variables de entorno exclusivamente

# Puerto del servidor
PORT=3000

# Entorno (development | production)
NODE_ENV=development

# CORS — origins permitidos (separados por coma)
CORS_ORIGIN=http://localhost:4200

# Firebase Admin SDK
# Obtener desde: Firebase Console → Configuración → Cuentas de servicio → Generar nueva clave privada
# Copiar projectId, clientEmail y privateKey del JSON descargado
# En privateKey: reemplazar saltos de línea reales por \n literales
FIREBASE_PROJECT_ID=appnotesbg-app
FIREBASE_CLIENT_EMAIL=firebase-adminsdk-xxxxx@appnotesbg-app.iam.gserviceaccount.com
FIREBASE_PRIVATE_KEY="-----BEGIN RSA PRIVATE KEY-----\nMIIEowIBAAK...\n-----END RSA PRIVATE KEY-----\n"

# Algolia (Admin API Key — NUNCA exponer en frontend)
ALGOLIA_APP_ID=your_app_id
ALGOLIA_ADMIN_API_KEY=your_admin_api_key
ALGOLIA_INDEX_NAME=notes_dev

# Google Gemini API (post-MVP)
GEMINI_API_KEY=
